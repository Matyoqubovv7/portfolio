# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB (optional - falls back to JSON storage if not provided)
MONGODB_URI=your_mongodb_uri_here

# Client URL (for CORS)
CLIENT_URL=http://localhost:5173
